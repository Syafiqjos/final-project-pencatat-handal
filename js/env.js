let IS_ORTHOGRAPHIC = false; // if true use perspective
let ENABLE_FOG = false; // enable fogging
let ENABLE_SHADOW = false; // enable shadow
let ENABLE_AUTOPILOT = false;  // enable autoplay
let ENABLE_BACKGROUND_MUSIC = false; //enable background music
let AUTOPILOT_ERROR = -0.08; // float 0.0 - 1.0 -> 0 jago, 1 cupu

let PLACEMENT_MODE = false; // enable birdeye view
let PLACEMENT_MODE_HEIGHT = 120; // height of birdeye view

let TOWER_ORIGINAL_HEIGHT = 1; // height of each layer
let TOWER_ORIGINAL_SIZE = 5; // original width and height of a box
let TOWER_ORIGINAL_OFFSET = -10; // offset of new spawned box layer, must negative
let TOWER_ORIGINAL_SPEED = 0.008; // new spawned tower movement speed

let TOWER_OFFSET_X = -12; // main tower offset x
let TOWER_OFFSET_Y = 0; // main tower offset y
let TOWER_OFFSET_Z = -5; // main tower offset z